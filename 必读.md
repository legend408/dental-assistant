# 牙科诊疗助手｜必读

> 这是本项目的长期上下文文件。每次开始新的对话或继续开发前，先阅读本文件。

## 当前状态

- 更新时间：2026 年 7 月 25 日
- 当前阶段：计划已重排，尚未开始正式编码
- 已完成：阅读并整理《基于通用大模型的健康诊疗系统_总体规划 (1).pptx》；完成范围裁剪与五周计划重排
- 当前工作区：仅有项目规划文档，`frontend/`、`backend/`、`docs/` 目录尚未建立
- Git 当前分支：`master`
- GitHub 远程仓库：尚未配置
- 说明：原定 7 月 11 日—17 日的第一周计划未执行，已作废，改用下方"五周计划"
- 下一步：执行第 1 周（7 月 25 日—7 月 31 日）任务，从 7 月 25 日的环境准备开始

## 开发者情况

- 学习阶段：大一计算机专业学生
- 已学基础：C、C++、Python 基础
- 暂未系统学习：Web 前端、后端工程、数据库、部署和大模型应用开发
- 每日可投入时间：4 小时以上
- 学习方式：希望边做项目边学习，不希望只看理论或一次性获得大量代码
- 讲解要求：使用初学者能理解的语言，先解释概念，再操作，再说明错误原因
- 开发目标：2026 年 8 月 31 日前完成 Web 端牙科诊疗助手并上线
- 截止日期性质：自定目标，非硬性考核。但**按硬日期推进**，避免无限延期；8 月 31 日后如需增强功能，再另行规划
- 当前重点：先做可运行的 Web MVP，再逐步增强功能
- 协作偏好：按周拆分任务，每周单独建立一个对话；总规划会话负责整体规划

## 给新对话的固定说明

开始工作前，请先读取本文件，不要要求用户重复介绍项目背景。读取后应先确认：

1. 用户是一名只具备 C、C++、Python 基础的大一学生；
2. 本项目采用边做边学的方式；
3. 如果是每周开发会话，只负责本周任务，不擅自改变总体规划；如果是总规划会话，则负责维护整体计划；
4. 解释代码时要说明代码放在哪里、如何运行、预期看到什么结果；
5. 每完成一个小步骤，都要进行验证，并记录遇到的问题；
6. 不要直接跳到复杂框架、复杂部署或大规模代码，先保证最小示例跑通。

如果用户没有指定任务，先查看"当前状态"和本周计划，再从下一个未完成的验收项开始。

## 项目目标

在 2026 年 8 月 31 日前完成一个可演示、可部署的 Web 端牙科诊疗辅助系统 MVP。

系统定位为辅助诊疗工具，不替代医生诊断。所有 AI 输出都必须经过医生审核、修改和确认后才能作为正式报告发布。

## 第一版范围（2026 年 7 月 25 日裁剪确认）

第一版只做 Web 端，不做微信小程序和 App。

核心原则：**做一条窄但完整、能演示的链路，不做三个半成品。**

### 必须保留：核心闭环

```text
患者填写牙齿不适描述
    ↓
后端调用大模型，生成病历摘要 + 风险提示
    ↓
医生查看病例，审核并修改摘要
    ↓
医生确认发布
    ↓
患者查看医生确认后的摘要和就医建议
```

这条链路走通，"AI 辅助 + 医生把关"的产品定位即成立。

### 降级处理

| 原计划 | 第一版实际做法 |
| --- | --- |
| 管理端 Web 界面 | 砍掉界面，用一个写死的管理员账号，数据直接看数据库 |
| 角色权限系统 | 三个角色硬编码，登录时区分，不做权限表 |
| MySQL | 全程使用 SQLite，部署也用 SQLite |
| MinIO 文件存储 | 后端本地目录存文件 |
| Redis 缓存 | 不用，第一版没有需要缓存的场景 |
| 操作审计日志 | 简化为一张 `ai_logs` 表，记录调用时间、输入、输出 |
| 图片和 PDF 上传 | 排到第 4 周；若时间不足，降级为"只上传保存，不做解析" |

### 明确砍掉（第一版不做）

- 知识库、Milvus 向量检索
- 注册流程、邮箱验证、找回密码
- 微信小程序、移动 App
- 复杂的报告模板和导出功能

## 技术路线

- 前端：Vue 3 + Vite，使用 JavaScript，不引入 TypeScript
- UI：Element Plus
- 路由：Vue Router
- 后端：Python FastAPI + Uvicorn
- 数据库：SQLite（第一版全程使用，包括部署）
- 文件存储：后端本地目录
- AI：后端统一封装一个模块调用大模型 API，前端不直接调用大模型
- 大模型 API 具体选哪家：**待定，第 3 周开始前确定**（依据支付方式和预算）
- 第一版不使用：MySQL、MinIO、Redis、Milvus

## 五周计划（2026 年 7 月 25 日—8 月 31 日）

### 铁律

- 每周结束时，系统都必须处于"能跑、能演示"的状态。
- 哪一周超时，就把该周剩余功能往后放，**不允许挤压第 5 周的部署时间**。
- 没有部署上线的项目等于没做完。
- 8 月 29 日—31 日为缓冲期，**不排新功能**。

### 第 1 周：7 月 25 日—7 月 31 日｜跑通开发链路

周目标：

```text
浏览器打开 Vue 页面 → 点击按钮 → Vue 调用 FastAPI → FastAPI 返回 JSON → Vue 显示结果
```

本周不接触 AI，目标是让开发者真正理解"浏览器 → 前端 → 后端 → 数据库"这条路。

- **7 月 25 日（六）｜环境与 Git**
  学习：前端、后端、数据库、HTTP、接口、JSON 的概念；Git 基本命令。
  完成：确认 Node.js、Python、VS Code、Git 可用；建立 `frontend/`、`backend/`、`docs/` 目录；写项目 README；`git init` 并完成第一次提交。
  验收：能说明一次请求如何从浏览器到达后端；能用 `git log` 看到第一次提交。

- **7 月 26 日（日）｜Vue 前端**
  学习：HTML、CSS、JavaScript 基础；Vue 组件、模板、点击事件。
  完成：创建 Vue 3 + Vite 项目；启动开发服务器；做出首页，放置患者端、医生端、管理端三个入口。
  验收：浏览器能打开页面，点击按钮后页面内容发生变化。

- **7 月 27 日（一）｜FastAPI 后端**
  学习：Python 虚拟环境、FastAPI、路由、GET 请求、JSON 响应。
  完成：创建虚拟环境；安装 FastAPI 和 Uvicorn；创建 `backend/main.py`；实现 `/api/health` 接口。
  验收：能启动后端，并打开 `http://127.0.0.1:8000/docs`。

- **7 月 28 日（二）｜前后端通信**
  学习：`fetch`、请求与响应、CORS 是什么以及为什么会报错。
  完成：Vue 页面增加"检查系统状态"按钮，请求 `/api/health`，把后端返回内容显示在页面上。
  验收：点击按钮后页面显示"后端运行正常"。

- **7 月 29 日（三）｜数据库与表结构**
  学习：数据库、数据表、主键、外键、一对多关系。
  完成：用 SQLite 建立本地数据库；设计 `users`、`patient_profiles`、`case_records`、`medical_summaries`、`ai_logs` 五张表；插入并查询一条测试病例；画出初版关系图。
  验收：能插入并查询一条测试病例。

- **7 月 30 日（四）｜页面骨架**
  学习：Vue Router、多页面结构。
  完成：创建患者端、医生端、管理端三个页面骨架，能通过路由互相跳转。
  验收：三个页面都能打开，导航可用。

- **7 月 31 日（五）｜周总结**
  完成：建立 `docs/project-status.md` 和 `docs/decisions.md`；整理本周遇到的问题；完成一次完整演示（前端 + 后端 + 数据库）；提交 Git。
  验收：见下方第 1 周验收清单。

#### 第 1 周验收清单

- [ ] Vue 前端可以启动
- [ ] FastAPI 后端可以启动
- [ ] 前端可以成功调用后端
- [ ] `/api/health` 接口可用
- [ ] FastAPI 自动文档可打开
- [ ] SQLite 数据库可用，能插入查询
- [ ] 五张表已设计
- [ ] 三个角色页面骨架完成，路由可用
- [ ] README、`docs/project-status.md`、`docs/decisions.md` 已建立
- [ ] 至少完成 5 次 Git 提交
- [ ] GitHub 远程仓库已配置并成功推送

### 第 2 周：8 月 1 日—8 月 7 日｜数据真正打通

- 患者端表单提交 → 后端接收 POST → 写入 SQLite
- 医生端列表页读取数据库，显示病例列表
- 病例详情页
- 最简单的登录：账号密码 + 角色区分（患者 / 医生 / 管理员）
- 周末验收：患者能提交一条病情描述，医生能在自己的页面上看到它

### 第 3 周：8 月 8 日—8 月 14 日｜接入大模型

- 第 3 周开始前：确定大模型 API 供应商，拿到 Key（Key 放 `.env`，不进 Git）
- 后端新增 AI 模块：输入患者描述，输出结构化病历摘要 + 风险提示
- 结果写入 `medical_summaries` 表，调用记录写入 `ai_logs` 表
- 前端展示 AI 生成的摘要
- 周末验收：患者提交描述后，能看到一份 AI 自动生成的病历摘要

### 第 4 周：8 月 15 日—8 月 21 日｜医生审核闭环

- 医生编辑并保存 AI 摘要
- 医生"确认发布"，患者端才可见
- 生成术后说明、复诊建议
- 图片 / PDF 上传（做不完就降级为只保存不解析）
- 用 Element Plus 统一整理界面
- 周末验收：核心闭环全部走通，界面不再是裸 HTML

### 第 5 周：8 月 22 日—8 月 28 日｜部署上线

- 选定部署平台，前端打包，后端上线
- 准备脱敏演示病例数据
- 整理 README 和使用说明
- 周末验收：从公网地址能打开并完成一次完整演示

### 缓冲期：8 月 29 日—8 月 31 日

- 修 bug、录演示视频、准备讲解材料
- 不排新功能

## 对话协作规则

### 用户新增的协作约束

- 每进行一个开发步骤前，必须先用初学者能理解的语言讲清楚这一步是什么、为什么做、文件放在哪里以及预期结果。
- 在用户确认已经理解当前步骤之前，不进行下一步开发操作。
- 每个步骤都采用"先讲解 → 用户确认理解 → 再操作 → 操作后验证"的顺序。
- 讲解应使用人话，避免只给结论、只贴代码或一次性提供大量代码。
- 用户提出的新约束、协作要求和长期有效的偏好，必须及时记录到本文件。
- 每次回答用户问题前，先检查对应周的记录，确认之前讲过的知识点是否已经记录；如果没有，先补充记录，再回答。
- 每完成一个步骤，都要说明实际修改了什么、验证结果是什么、遇到了什么问题以及下一步是什么。
- 遇到需要扩大范围或更换技术的想法，先记录，回总规划会话讨论，不在开发会话中直接决定。

新的对话框不会自动读取其他对话的完整聊天记录。开始新对话时，先要求读取：

1. `必读.md`
2. `docs/project-status.md`（第 1 周末建立）
3. `docs/decisions.md`（第 1 周末建立）

### 会话分工

- 总规划会话：负责整体规划、阶段目标、周计划、范围控制、技术路线和关键决策。
- 每周新会话：负责对应周的学习、编码、调试、测试和周总结。
- 每周会话开始前：先读取本文件和相关进度文档。
- 每周会话结束后：更新进度文档，并把未解决问题交回总规划会话。
- 不在每周会话中擅自改变总体技术路线或扩大第一版范围。

## 这个文件怎么使用

### 新建每周对话时

```text
请先读取项目目录中的"必读.md"。这是第 N 周开发会话，请按照其中的用户情况、技术路线和会话分工，带我边学边完成本周任务。先告诉我今天要做什么、为什么做、文件放在哪里，然后再开始操作。
```

### 每天开始时

```text
请读取"必读.md"，继续今天的任务。先检查当前代码和昨天的进度，再给我一个今天的任务清单。
```

### 每天结束时

```text
请总结今天完成的内容、修改的文件、遇到的问题和明天的任务，并更新相关进度文档。
```

### 需要修改总体计划时

回到总规划会话：

```text
请根据当前开发进度，重新评估总体计划，并同步更新"必读.md"。
```

### 文档更新原则

- 本文件保存稳定信息：项目目标、用户情况、技术路线、范围、五周计划、会话分工和长期规则。
- `docs/project-status.md` 保存当前进度、已完成事项和阻塞问题。
- `docs/weekly/week-N.md` 保存每周详细学习和开发记录。
- 不要把每天的聊天内容全部复制进本文件，避免文件过长。

## GitHub 同步规则

每次修改代码或项目文档后，按以下顺序操作：

```bash
git status
git diff
git add <本次相关文件>
git commit -m "清晰描述本次修改"
git push origin <当前分支>
```

注意：

- 第一次推送前必须配置远程仓库 `origin`
- 不提交密码、API Key、患者真实资料或 `.env` 文件；第一次提交前就要写好 `.gitignore`
- 每次提交只包含本次相关修改
- 如果 GitHub 连接未配置，先暂停推送并补充远程仓库信息

## 当前未解决事项

- [ ] 创建 GitHub 仓库并配置 `origin`（第 1 周内完成）
- [ ] 建立 `docs/project-status.md`（7 月 31 日）
- [ ] 建立 `docs/decisions.md`（7 月 31 日）
- [ ] 确定使用哪一个大模型 API（第 3 周开始前，即 8 月 7 日前）
- [ ] 确定正式部署平台和域名（第 5 周开始前，即 8 月 21 日前）
- [ ] 准备脱敏的演示病例数据（第 5 周）
